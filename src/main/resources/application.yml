# 应用名称
server:
  port: 8080

# 应用名称
spring:
  application:
    name: spring-cloud-rate-limiter
  redis:
    database: 0
    password:
    port: 6379
    host: 127.0.0.1
    lettuce:
      shutdown-timeout: 100ms
      pool:
        min-idle: 5
        max-idle: 10
        max-active: 8
        max-wait: 1ms

